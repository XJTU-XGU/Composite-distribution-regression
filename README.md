# Composite distribution regression via structured transport map

This repository contains the code for the paper **"Composite distribution regression via structured transport map"**. Simulation results and reproduction instructions are in `simulation/`; real-data results and reproduction instructions are in `real_data/`. Please see the README file in each subdirectory for the corresponding details.
